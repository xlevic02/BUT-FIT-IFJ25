import "ifj25" for Ifj
class Program {

static main() {
    var s
    var idx
    var code
    var x

    s = "A"
    idx = 0
    code = Ifj.ord(s, idx)  // int

    x = code                // int

    x = Ifj.length(x)       // int › 25

    __d = Ifj.write(x)
}
}
