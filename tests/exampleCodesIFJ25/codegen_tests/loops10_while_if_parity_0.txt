import "ifj25" for Ifj
class Program {

static main() {
    var i

    i = 0
    while (i < 6) {
        if ((i + i) == (i * 2)) {
            __d = Ifj.write("ok ")
        } else {
            __d = Ifj.write("bad ")
        }

        __d = Ifj.write(i)
        __d = Ifj.write("\n")

        i = i + 1
    }
}
}
