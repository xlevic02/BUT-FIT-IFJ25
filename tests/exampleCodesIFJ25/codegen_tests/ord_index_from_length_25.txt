import "ifj25" for Ifj
class Program {

static main() {
    var s
    var idx
    var r

    s = "abc"
    idx = 1
    idx = "1"        // string

    r = Ifj.ord(s, idx)   // index nie je int › 25

    __d = Ifj.write(r)
}
}
